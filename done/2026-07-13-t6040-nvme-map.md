# T6040 J614s ANS/NVMe map (2026-07-13)

## Result

The complete storage-controller map was read from the live J614s ADT through
the m1n1 proxy without reading or writing any MMIO. T6040 follows the T8140
ANS2 shape at different addresses. It uses a power-managed CoastGuard SART v3
and the NVMe controller's embedded NVMMU; there is no Apple DART in this path.

Linux commit `9cf4a92fa16f` adds the following nodes to `t6040.dtsi`, all
disabled so the standard DTB performs no new hardware access:

| Component | Address / size | IRQs | Source |
|---|---:|---:|---|
| ANS ASC control | `0x209600000 / 0x4000` | — | ADT `/arm-io/ans` reg 0 |
| ANS mailbox | `0x209608000 / 0x4000` | `1530..1533` | ASC + `0x8000`; ADT raw order `[1531,1530,1533,1532]` |
| SART v3 | `0x20dc50000 / 0xc000` | — | ADT `/arm-io/sart-ans` reg 0, `sart-version = 3` |
| SART power control | `0x20dcc13e8 / 0x4` | — | ADT reg 2 `0x20dcc0000` + `sart-power-reg-offset = 0x13e8` |
| NVMe + NVMMU | `0x20dcc0000 / 0x60000` | `2583` | ADT `/arm-io/ans` reg 3 and `nvme-interrupt-idx = 4` |

The ANS node's role is `ANS2`; its first four raw IRQs use Apple's not-empty
first ordering and were normalized to the Linux mailbox order. The power path
is `ps_ans` plus `ps_apcie_phy_sw`; the latter already depends on both storage
PCIe system domains. The provisional compatibles follow yuka's existing T8140
WIP nodes. ANS2 follows T8103, but the probe proved that T8140 SART is not
safely compatible with the unmodified T6000 driver despite sharing its v3
entry layout: the register file must first be activated separately.

The current standard `t6040-j614s-dcuart.dtb` keeps every storage node disabled,
so the extra resource description performs no hardware access. Its SHA-256 is
`bc1534d214d6cde172b1aad1b78f85104f9c0a9790a6fabd1e9990fe950ff41b`;
it was boot-verified back to the build #15 BusyBox shell after the probe.

## Gated first-probe artifact

`dts/t6040-j614s-dcuart-nvme.dts` includes the standard remote-console source
and only changes the three storage nodes to `okay`.
`scripts/t6040-build-nvme-candidate.sh` can build the original built-in stack or
a staged image (`PROBE_MODE=staged`) with SART built in and the NVMe drivers as
modules. It never replaces the known-good default `Image`, accepts an isolation
DT through `SOURCE=`, and labels every output as gated without replacing the
default artifacts.

### Initial pre-probe artifacts

The maintainer approved the original built-in image and full DTB with hashes:

- `Image-nvme`: `d8839ae3c92bb1ed5a0156f53f98571f299d3e34aea4d3bf554d563aace8bf0f`
- `t6040-j614s-dcuart-nvme.dtb`:
  `15cd8a113f852adc58bc5af7d394038f7bdc3d641baa1b684b9496f57f60f8b9`

The builder verified `CONFIG_BLK_DEV_NVME=y`, `CONFIG_NVME_APPLE=y`, and
`CONFIG_APPLE_SART=y`; the image contained no stale `MTPDBG` instrumentation.
The known-good build #15 default artifacts were then byte-identical:

- `Image`: `14da8640398fc64b89d9241a75be0ffc8d4260b681068a3c27251cc79c3abaf4`
- `t6040-j614s-dcuart.dtb`:
  `a99ad7c3f304198280814de1e4a31d83c268751af608afad7003aa982a69f65a`

`patches/t8140-ans-bindings.patch` documents the three initial T8140 WIP
compatible combinations. The later power-control correction is split into
`patches/t8140-sart-power-bindings.patch` so reused build trees upgrade cleanly
from that initial state.

## Approved first-probe result

The approved built-in image plus full DTB handed off successfully but never
reached the Linux DockChannel shell. KIS disconnected after the watchdog-reset
interval. The unchanged standard image and DTB immediately booted to BusyBox,
excluding a tether, PMGR, or general kernel regression.

A staged rebuild put `nvme-core` and `nvme-apple` in the initramfs as modules.
The same full DT still reset before userspace without either module loaded, so
the failure preceded Apple NVMe probe. Two cumulative isolation DTs then made
the boundary exact:

- ASC mailbox only reached BusyBox and reported the gated mailbox model;
- ASC mailbox plus SART, with NVMe disabled, reset before userspace.

Therefore the first probe stopped at SART. The namespace never enumerated. No
`fdisk`, partition, mount, filesystem, flush, or other disk command was issued,
and no ANS/NVMe register write was reached. The target was returned to the
standard build #15 BusyBox image after the investigation.

## CoastGuard power-control finding

A second read-only m1n1 ADT dump, with no MMIO access, showed that
`/arm-io/sart-ans` has all of the following:

- `sart-version = 3`, `compatible = "sart", "coastguard"`;
- the boolean `sart-power-managed`;
- `sart-power-reg-offset = 0x13e8`;
- reg 0 `0x20dc50000 / 0xc000` for entries; and
- reg 2 `0x20dcc0000 / 0x4000`, placing the control register at
  `0x20dcc13e8`.

Static disassembly of this machine's paired AppleSART driver from the macOS
26.5.2 (25F84) kernelcache established the exact lifecycle without touching
the target. `IOCoastGuardSARTMapper` maps provider memory index 2. On its first
active mapping it repeatedly writes `0`, delays 100 us, and reads back until
the register is `0`. On the last unmap it repeatedly writes `1`, delays 100 us,
and reads back until it is `1`. Both paths are protected by a lock and active
mapping reference count. No Apple binary or extracted kernelcache is stored in
this repository.

This corrects the initial T6000 fallback audit: the v3 entry format is shared,
but reading the entry register file while CoastGuard reports inactive triggers
the observed reset.

## Approved CoastGuard retry

`patches/t8140-sart-power-bindings.patch` makes `apple,t8140-sart` a standalone
compatible with named `sart` and `power` resources.
`patches/t8140-sart-power-managed.patch` implements the reviewed protocol. The
maintainer explicitly approved writes `0` and `1` at `0x20dcc13e8`, including
the 100 us delay and matching readback. The retry used cumulative diagnostics:

- handshake-only (CoastGuard transition, no entry scan) still watchdog-reset;
- zero-MMIO SART probe reached BusyBox and logged that all SART MMIO was
  skipped; and
- `patches/t8140-sart-defer-scan.patch` reached BusyBox with the real driver.

The production correction leaves old SART variants unchanged, but defers the
power-managed SART's one-time protected-entry scan until the first allow-list
operation has acquired an active reference. It serializes that scan and avoids
reactivating an unused CoastGuard during shutdown. The SART-only DT and then
the full DT with both NVMe modules unloaded reached BusyBox. This proves the
CoastGuard probe-time blocker is fixed.

Loading `nvme-core.ko` returned zero. Loading `nvme-apple.ko` caused a watchdog
reset before a namespace appeared. `patches/t6040-nvme-trace-debug.patch` and
`patches/t8140-sart-trace-debug.patch` add yielding, bring-up-only checkpoints.
The exact streamed tail was:

```text
[100.830141] nvme-apple 20dcc0000.nvme: trace: platform probe entered
[100.936673] nvme-apple 20dcc0000.nvme: trace: allocation entered
[101.040735] nvme-apple 20dcc0000.nvme: trace: power domains attached
[101.144709] nvme-apple 20dcc0000.nvme: trace: MMIO resources mapped
[101.248723] nvme-apple 20dcc0000.nvme: trace: SART supplier acquired
[101.356801] nvme-apple 20dcc0000.nvme: trace: queues allocated
[101.460793] nvme-apple 20dcc0000.nvme: trace: RTKit client initialized
[101.564680] nvme-apple 20dcc0000.nvme: trace: NVMe controller object initialized
[101.668752] nvme-apple 20dcc0000.nvme: trace: reset work queued
[101.669750] nvme-apple 20dcc0000.nvme: trace: reset work entered
[101.772796] nvme-apple 20dcc0000.nvme: trace: before ANS CPU control read
```

The next operation is `readl()` at physical `0x209600044`. It resets before any
CoastGuard write, SART entry access, ASC RUN write, mailbox traffic, or NVMe
command.

## ANS PMGR finding and prepared retry

The live `/arm-io/ans` ADT has empty `clock-gates` and `power-gates` marker
properties; m1n1's NVMe path does not explicitly enable ANS. Its accessibility
therefore depends on boot PMGR state. ADT-derived, read-only PMGR inspection
showed:

```text
ANS            @ 0x502280370 = 0x0f0000ff
APCIE_SYS_ST0  @ 0x502280380 = 0x1000030f
APCIE_SYS_ST1  @ 0x502280388 = 0x1000030f
APCIE_PHY_SW   @ 0x502280398 = 0x1400024f
```

Firmware leaves ANS target/actual state at `0xf` with AUTO_ENABLE clear. The
generic T6041 PMGR probe enables auto-PM on that already-active domain before
the staged NVMe driver runs. `patches/t6040-pmgr-ans-no-auto.patch` adds an
NVMe-build-only exception; `dts/t6040-j614s-dcuart-nvme-ans-hold.dts` selects
the same pre-existing bring-up property independently. The build harness now
applies and removes this patch deterministically alongside independent SART
and NVMe trace cleanup.

The DT-property variant booted to BusyBox. Its module result is inconclusive:
an over-broad relay included historical `pmgr` messages, so the fatal-window
checkpoint had not drained before the transport disconnected. The m1n1 proxy
then remained unresponsive after a kisd restart and explicit DebugUSB re-entry;
live work stopped under the rig rule. Prepared, unbooted retry artifacts are:

- `Image-sart-trace`:
  `0c4880522c4793629f6e9a25ea164c911801e67754ae43cd3a6b5b274e20e8e6`;
- `nvme-core.ko`:
  `5e61ba16697daa382c5bb614fdaf3d5948a3818c11a630d5766e3b88ead36af7`;
- `nvme-apple.ko`:
  `189c472d561a7511f684a097f695a6c2aa65ef943a1deadbb37e2de8d726fd5e`;
- `t6040-j614s-dcuart-nvme-ans-hold.dtb`:
  `cc2c48e30a09080117222d5f4c9fb795dfd6bb338d2cf26b23085ad947ffbefb`;
- `initramfs-dcuart-nvme-ans-hold.cpio.gz`:
  `ae80f82033e5f0d683ac09a3fa61e67c3c63e8a7c1be7593a0fd7fe687732873`.

None has been booted as a set. On the next healthy DebugUSB session, relay only
new `trace:` lines, load core then Apple, and stop again at the first new fatal
boundary.

## Remaining ANS/NVMe audit

The other fallback choices were compared line-by-line with both Linux and
m1n1:

- `apple,asc-mailbox-v4` selects the standard ASC FIFO layout used by the live
  ADT's four mailbox interrupts;
- the T8140 SART uses the v3 address/size/flags layout, now combined with its
  separately derived CoastGuard lifecycle instead of a T6000 fallback;
- `apple,t8103-nvme-ans2` selects 64-entry linear submission queues and the
  embedded NVMMU; and
- Linux's boot-status, queue-doorbell, controller, and NVMMU programming
  offsets match m1n1's implementation.

There is one old diagnostic-only source discrepancy: m1n1 defines the TCB
status read as `0x29120`, while Linux has used `0x28120` since its original
2022 Apple NVMe driver. m1n1 already used its value on pre-T6040 machines, so
this is not evidence of a T6040 register move. The read only warns after a TCB
invalidation; it does not select or trigger invalidation and does not change
the first-probe plan. Do not resolve it by probing either offset live without a
separately reviewed register source.

Proceeding beyond the SART-only retry invokes these bounded driver writes:

- PMGR transitions for `ps_ans` and `ps_apcie_phy_sw`;
- CoastGuard state requests at exactly `0x20dcc13e8`: `0` for active and `1`
  for inactive, with 100 us delay and matching readback;
- SART v3 entry programming below `0x20dc500c0`, with physical address and size
  values chosen from dynamically allocated RTKit buffers;
- ASC RUN at `0x209600044` and mailbox traffic in `0x209608000..0x209608fff`;
- NVMe/NVMMU setup registers below the ADT-backed
  `0x20dcc0000..0x20dd1ffff` resource, including dynamically allocated queue and
  TCB addresses; and
- ordinary NVMe identify/admin commands after firmware boot.

The normal ANS/NVMe behavior and the later CoastGuard transitions are both
approved. Both pre-module gates passed. Resume with the prepared ANS-hold trace
set only after DebugUSB recovers; if controller boot completes, the read-only
enumeration command set is:

```sh
insmod /lib/modules/nvme-core.ko
insmod /lib/modules/nvme-apple.ko
dmesg
cat /proc/partitions
find /sys/class/nvme -maxdepth 3 -type f -print
for d in /sys/class/nvme/nvme*; do
    for n in model serial firmware_rev state; do
        f="$d/$n"
        [ -r "$f" ] && echo "$f: $(cat "$f")"
    done
done
fdisk -l /dev/nvme0n1
```

Stop after collecting that output even if the namespace looks healthy.
No format, namespace write, filesystem repair, mount, or flush command is
permitted. BusyBox includes mount applets, but their presence is not permission
to use them on the SSD.
